@extends('admin.layouts.admin')

@section('title', 'TripWise — Evaluation Reports')

@section('content')
<!-- Breadcrumb -->
<div class="breadcrumb">
    <a href="{{ route('admin.dashboard') }}">Home</a>
    <span>/</span>
    <a href="{{ route('admin.reports.index') }}">Reports & Analytics</a>
    <span>/</span>
    <span>Evaluation Reports</span>
</div>

<!-- Page Header -->
<div style="display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:1.5rem;flex-wrap:wrap;gap:1rem;">
    <div>
        <h1 style="font-size:1.75rem;color:var(--primary);margin:0 0 0.25rem;">Evaluation Reports</h1>
        <p style="color:var(--text-muted);font-size:0.9rem;margin:0;">Generate reports from Peer Evaluation, Recognition, Succession Planning, and Feedback modules.</p>
    </div>
    <div style="display:flex;gap:0.75rem;flex-wrap:wrap;">
        <button class="btn btn-secondary" onclick="exportReport('pdf')"><i class="fas fa-file-pdf"></i> Export PDF</button>
        <button class="btn btn-secondary" onclick="exportReport('excel')"><i class="fas fa-file-excel"></i> Export Excel</button>
    </div>
</div>

<!-- Dashboard Stats Cards -->
<div class="summary-grid">
    <div class="summary-card">
        <div class="card-icon blue"><i class="fas fa-clipboard-check"></i></div>
        <div class="card-info">
            <h3>{{ $stats['evaluation'] }}</h3>
            <p>Evaluation Reports</p>
        </div>
    </div>
    <div class="summary-card">
        <div class="card-icon green"><i class="fas fa-trophy"></i></div>
        <div class="card-info">
            <h3>{{ $stats['recognition'] }}</h3>
            <p>Recognition Reports</p>
        </div>
    </div>
    <div class="summary-card">
        <div class="card-icon orange"><i class="fas fa-rocket"></i></div>
        <div class="card-info">
            <h3>{{ $stats['promotion'] }}</h3>
            <p>Promotion Reports</p>
        </div>
    </div>
    <div class="summary-card">
        <div class="card-icon purple"><i class="fas fa-comments"></i></div>
        <div class="card-info">
            <h3>{{ $stats['feedback'] }}</h3>
            <p>Feedback Reports</p>
        </div>
    </div>
</div>

<!-- Filters -->
<div class="table-card" style="margin-bottom:1.5rem;padding:1rem 1.25rem;">
    <form method="GET" action="{{ route('admin.reports.evaluation-reports') }}" class="filter-bar" style="margin-bottom:0;flex-wrap:wrap;">
        <input type="text" name="search" placeholder="Search evaluation reports..." value="{{ request('search') }}" style="padding:0.5rem 0.85rem;border:1px solid var(--border);border-radius:0.5rem;font-size:0.85rem;background:var(--white);color:var(--text-dark);font-family:'Inter',sans-serif;min-width:200px;">
        <select name="category" style="padding:0.5rem 0.85rem;border:1px solid var(--border);border-radius:0.5rem;font-size:0.85rem;background:var(--white);color:var(--text-dark);font-family:'Inter',sans-serif;min-width:160px;">
            <option value="">All Categories</option>
            <option value="evaluation" {{ request('category') === 'evaluation' ? 'selected' : '' }}>Peer Evaluation</option>
            <option value="recognition" {{ request('category') === 'recognition' ? 'selected' : '' }}>Recognition</option>
            <option value="succession" {{ request('category') === 'succession' ? 'selected' : '' }}>Succession Planning</option>
            <option value="peer_evaluation" {{ request('category') === 'peer_evaluation' ? 'selected' : '' }}>Feedback</option>
        </select>
        <div style="margin-left:auto;display:flex;gap:0.5rem;">
            <button type="submit" class="btn btn-primary" style="padding:0.5rem 1rem;font-size:0.85rem;"><i class="fas fa-search"></i> Search</button>
            <a href="{{ route('admin.reports.evaluation-reports') }}" class="btn btn-secondary" style="padding:0.5rem 1rem;font-size:0.85rem;"><i class="fas fa-undo"></i> Reset</a>
        </div>
    </form>
</div>

<!-- Reports Table -->
<div class="table-card">
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:1rem;flex-wrap:wrap;gap:0.75rem;">
        <h3 style="margin:0;"><i class="fas fa-list"></i> Evaluation Reports</h3>
        <span style="font-size:0.85rem;color:var(--text-muted);">Showing {{ $reports->firstItem() ?? 0 }} to {{ $reports->lastItem() ?? 0 }} of {{ $reports->total() }} results</span>
    </div>
    <div class="table-wrapper">
        <table>
            <thead>
                <tr>
                    <th>Report ID</th>
                    <th>Report Name</th>
                    <th>Report Category</th>
                    <th>Date Generated</th>
                    <th>Generated By</th>
                    <th>Status</th>
                    <th style="text-align:center;">Actions</th>
                </tr>
            </thead>
            <tbody>
                @forelse($reports as $report)
                <tr>
                    <td><span style="font-size:0.8rem;color:var(--text-muted);font-family:monospace;">#RPT-{{ str_pad($report->id, 6, '0', STR_PAD_LEFT) }}</span></td>
                    <td><strong>{{ $report->name }}</strong></td>
                    <td><span class="status-badge status-review">{{ ucfirst(str_replace('_', ' ', $report->category)) }}</span></td>
                    <td>{{ $report->generated_at ? $report->generated_at->format('M d, Y') : '-' }}</td>
                    <td>{{ $report->generatedBy->name ?? 'System' }}</td>
                    <td><span class="status-badge status-{{ $report->status === 'generated' ? 'success' : ($report->status === 'archived' ? 'pending' : 'review') }}">{{ ucfirst($report->status) }}</span></td>
                    <td style="text-align:center;">
                        <button class="icon-btn" title="View" onclick="showToast('View report')"><i class="fas fa-eye"></i></button>
                        <button class="icon-btn" title="Export PDF" onclick="exportReport('pdf')"><i class="fas fa-file-pdf"></i></button>
                        <button class="icon-btn" title="Export Excel" onclick="exportReport('excel')"><i class="fas fa-file-excel"></i></button>
                        <button class="icon-btn" title="Print" onclick="showToast('Print report')"><i class="fas fa-print"></i></button>
                    </td>
                </tr>
                @empty
                <tr>
                    <td colspan="7" style="text-align:center;padding:2rem;color:var(--text-muted);">No evaluation reports found.</td>
                </tr>
                @endforelse
            </tbody>
        </table>
    </div>
    <div style="margin-top:1rem;display:flex;justify-content:center;">
        {{ $reports->links() }}
    </div>
</div>

<!-- Charts -->
<div class="charts-grid" style="margin-top:1.5rem;">
    <div class="chart-card">
        <h3><i class="fas fa-chart-line"></i> Evaluation Trends</h3>
        <div class="chart-wrapper">
            <canvas id="evaluationTrendChart"></canvas>
        </div>
    </div>
    <div class="chart-card">
        <h3><i class="fas fa-chart-bar"></i> Recognition Trends</h3>
        <div class="chart-wrapper">
            <canvas id="recognitionTrendChart"></canvas>
        </div>
    </div>
    <div class="chart-card">
        <h3><i class="fas fa-chart-pie"></i> Promotion Analytics</h3>
        <div class="chart-wrapper">
            <canvas id="promotionAnalyticsChart"></canvas>
        </div>
    </div>
</div>

@endsection

@push('scripts')
<script>
function exportReport(format) {
    showToast('Exporting evaluation reports as ' + format.toUpperCase() + '...');
}

document.addEventListener('DOMContentLoaded', function() {
    const evaluationTrendCtx = document.getElementById('evaluationTrendChart');
    if (evaluationTrendCtx) {
        new Chart(evaluationTrendCtx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
                datasets: [{
                    label: 'Evaluations',
                    data: [12, 19, 15, 25, 22, 30],
                    borderColor: '#F44336',
                    backgroundColor: 'rgba(244, 67, 54, 0.1)',
                    fill: true,
                    tension: 0.4,
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                scales: { y: { beginAtZero: true } },
                plugins: { legend: { display: false } }
            }
        });
    }

    const recognitionTrendCtx = document.getElementById('recognitionTrendChart');
    if (recognitionTrendCtx) {
        new Chart(recognitionTrendCtx, {
            type: 'bar',
            data: {
                labels: ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun'],
                datasets: [{
                    label: 'Recognitions',
                    data: [8, 12, 10, 15, 18, 14],
                    backgroundColor: '#F44336',
                    borderRadius: 8,
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                scales: { y: { beginAtZero: true } },
                plugins: { legend: { display: false } }
            }
        });
    }

    const promotionAnalyticsCtx = document.getElementById('promotionAnalyticsChart');
    if (promotionAnalyticsCtx) {
        new Chart(promotionAnalyticsCtx, {
            type: 'doughnut',
            data: {
                labels: ['Ready', 'In Progress', 'Not Ready'],
                datasets: [{
                    data: [35, 45, 20],
                    backgroundColor: ['#10b981', '#f59e0b', '#ef4444'],
                    borderWidth: 0,
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: { legend: { position: 'bottom' } }
            }
        });
    }
});
</script>
@endpush
